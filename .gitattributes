# Normalize line endings to LF in the repo (stops CRLF<->LF churn on Windows).
* text=auto eol=lf

# Binary assets — never touch.
*.wav binary
*.png binary
*.jpg binary
*.pth binary
*.safetensors binary
